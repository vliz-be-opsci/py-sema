# Seagrass survey dataset

This example dataset accompanies the RO-Crate blueprint in `roc-me.yml`.
It describes a small transect survey that records seagrass counts by plot.

## Files
- `data/observations.csv`: raw observation log
- `data/summary.json`: basic statistics derived from the observations
- `scripts/analyze.py`: simple script that totals the counts
